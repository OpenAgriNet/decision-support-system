"""Moderation — decides whether an interaction may proceed.

Judges harm against configured policies over the enriched query and the intent
object. It does not classify: the intent object arrives already classified.
"""
