.vscode
.idea
.claude/settings.local.json
.DS_Store
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
# tests/
# Written by a local run, not source: evidence JSONL (docs/RUNNING.md) and the
# schema packs `scripts/fetch_schema_packs.py` downloads. The packs come from
# the network-specs repo — fetched, never committed here.
var/

# Local scratch — handoffs, notes, anything not for review
.scratchpad/

# Langfuse stack env template — the real .env.langfuse is ignored above.
!.env.langfuse.example
